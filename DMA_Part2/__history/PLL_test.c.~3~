#include <PLL_test.h>


void main()
{



   //Example blinking LED program
   while(pll_locked())
   {
      output_low(LED);
      delay_cycles(DELAY);
      output_high(LED);
      delay_cycles(DELAY);
   }

}
