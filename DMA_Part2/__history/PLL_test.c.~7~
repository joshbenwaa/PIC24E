#include <PLL_test.h>


void main()
{
   setup_oscillator(OSC_CRYSTAL,140000000,10000000);
   while(true)
   {
   //Example blinking LED program
      output_low(LED);
      delay_cycles(DELAY);
      output_high(LED);
      delay_cycles(DELAY);
   }

}
