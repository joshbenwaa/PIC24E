#include <FRC.h>


void main()
{

     setup_oscillator(OSC_INTERNALL, 100000000);

     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
      while(true)
   {
   }

}
