#include <FRC.h>


void main()
{

     setup_oscillator(OSC_CRYSTAL, 10000000);

     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
     output_toggle(LED);
      while(true)
   {
   }

}
